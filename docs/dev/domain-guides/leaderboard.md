# Leaderboard Domain Guide

> 문서 유형: Development Guide
> 상태: Draft
> 원본 위치: `docs/dev/domain-guides/leaderboard.md`
> 관련 도메인: User Point, ESG Grade, User, Department
> 최종 수정: 2026-06-12

## 개발 목표

리더보드는 사용자별 연간 누적 ESG 점수를 기준으로 순위를 보여주는 기능이다.

현재 구현 대상 API는 `GET /api/v1/leaderboard`이며, 리더보드 화면은 총 4개 파트로 구성될 예정이다. 이 문서는 현재까지 논의된 첫 번째 파트인 ESG 점수 상위 3명 조회 기능을 기준으로 정리한다.

## 핵심 개념

- 사용자의 활동에 따라 포인트가 지급된다.
- 1년 동안 누적된 포인트를 플랫폼에서는 `ESG 점수`라고 부른다.
- `user_points.current_point`는 현재 보유 포인트이다.
- `user_points.esg_score`는 연간 누적 ESG 점수이다.
- ESG 점수는 1년 단위로 초기화된다.
- 리더보드 순위는 `user_points.esg_score` 기준으로 산정한다.
- `user_points.esg_score`는 차감 대상이 아니므로 음수가 될 수 없다.
- 포인트 차감은 `current_point`에만 적용되며, `esg_score`는 연간 누적 점수로 유지된다.
- ESG 등급은 항상 `0 이상`의 `esg_score` 기준으로 산정한다.

## 첫 번째 파트: ESG 점수 TOP 3

화면에는 ESG 점수 기준 상위 1~3위 사용자를 보여준다.

표시 후보 데이터:

- 순위
- 사용자 ID
- 사용자 이름 또는 닉네임
- 부서명
- ESG 등급
- ESG 점수

조회 기준:

- `user_points.esg_score` 내림차순
- 상위 3명만 응답
- 삭제되었거나 비활성 처리된 사용자는 제외하는 방향이 적절함

## ESG 등급 기준

| 등급 | `esg_grade.grade_id` | ESG 점수 범위                             |
|---|---:|---------------------------------------|
| Lv1 | 1 | `0 <= user_points.esg_score <= 100`   |
| Lv2 | 2 | `101 <= user_points.esg_score <= 300` |
| Lv3 | 3 | `301 <= user_points.esg_score <= 700` |
| Lv4 | 4 | `701 <= user_points.esg_score`        |

## 순위 갱신 정책

리더보드 순위는 매주 월요일 오전 9시 기준으로 업데이트된 값을 화면에 보여준다.

따라서 API 호출 시점마다 실시간으로 `user_points`를 정렬하는 방식보다는, 매주 월요일 09:00에 계산된 순위 스냅샷을 저장하고 조회 API는 저장된 스냅샷을 반환하는 방식이 적절하다.

이유:

- 화면에 표시되는 순위가 한 주 동안 안정적으로 유지된다.
- "월요일 오전 9시 기준 순위"라는 정책을 명확하게 보장할 수 있다.
- API 요청마다 전체 사용자의 점수를 정렬하지 않아도 된다.

## 추천 구현 방식

1. 리더보드 스냅샷 테이블을 추가한다.
2. 매주 월요일 09:00에 스케줄러가 실행된다.
3. 스케줄러는 `user_points.esg_score` 기준으로 사용자 순위를 계산한다.
4. 계산된 상위 3명 데이터를 스냅샷 테이블에 저장한다.
5. `GET /api/v1/leaderboard`는 스냅샷 테이블에서 최신 리더보드 데이터를 조회한다.

## API

```http
GET /api/v1/leaderboard
```

예상 응답 구조:

```json
{
  "topRankers": [
    {
      "rank": 1,
      "userId": 1,
      "userName": "홍길동",
      "departmentName": "개발팀",
      "gradeId": 4,
      "gradeName": "Lv4",
      "esgScore": 1200
    }
  ]
}
```

응답 구조는 남은 3개 파트의 요구사항이 확정되면 함께 확장한다.

## 구현 전 확인 필요

- 동점자 순위 정책: 같은 ESG 점수일 때 공동 순위로 볼지, 특정 기준으로 순서를 나눌지 결정 필요
- 동점자 정렬 기준: 예를 들어 `esg_score DESC`, `user_id ASC`
- 스냅샷 보관 정책: 최신 1건만 유지할지, 주차별 이력을 보관할지 결정 필요
- 최초 스냅샷이 없는 경우: 빈 목록을 반환할지, 최초 1회 실시간 계산할지 결정 필요
- 리더보드 나머지 3개 파트의 데이터 기준 및 응답 구조 확정 필요

## 현재까지의 결론

현재 요구사항만 기준으로 보면 리더보드 첫 번째 파트는 `user_points.esg_score`를 기준으로 상위 3명을 산정하면 구현 가능하다.

다만 "매주 월요일 오전 9시 기준 순위"라는 정책이 있으므로, 단순 실시간 조회보다 스냅샷 저장 방식으로 구현하는 것이 요구사항에 더 잘 맞는다.
