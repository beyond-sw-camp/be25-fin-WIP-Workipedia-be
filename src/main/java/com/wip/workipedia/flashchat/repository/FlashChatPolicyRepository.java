package com.wip.workipedia.flashchat.repository;

import com.wip.workipedia.flashchat.domain.FlashChatPolicy;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FlashChatPolicyRepository extends JpaRepository<FlashChatPolicy, Long> {
}
