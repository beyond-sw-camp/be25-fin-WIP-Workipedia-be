spring:
  application:
    name: Workipedia

  config:
    import: optional:file:.env[.properties]

  autoconfigure:
    exclude:
      # 아래 3개는 회원가입 DB 저장을 활성화하기 위해서 삭제해야 함
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
      # 아래 2개는 Redis 기능을 활성화할 때 삭제해야 함
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  logging:
    level:
      '[org.springframework.web]': info
      '[org.springframework.security]': info
      '[org.mariadb.jdbc.client.impl]': info
      '[org.springframework.data.redis]': info
      '[io.lettuce.core]': info

  management:
    endpoints:
      web:
        exposure:
          include: health,info,metrics

    endpoint:
      health:
        show-details: always

    metrics:
      tags:
        application: Workipedia

# DB/JPA/Flyway exclude 제거 후 자동설정 활성화 시 필요한 설정
# spring:
#   datasource:
#     url: jdbc:mariadb://localhost:${MARIADB_PORT:3306}/${MARIADB_DATABASE:workipedia}
#     username: ${MARIADB_USER:wip}
#     password: ${MARIADB_PASSWORD:1q2w3e4r!}
#     driver-class-name: org.mariadb.jdbc.Driver
#
#   jpa:
#     hibernate:
#       ddl-auto: ${SPRING_JPA_DDL_AUTO:validate}
#     open-in-view: false
#     properties:
#       hibernate:
#         format_sql: true
#
#   flyway:
#     enabled: true
#     locations: classpath:db/migration
#     baseline-on-migrate: false
